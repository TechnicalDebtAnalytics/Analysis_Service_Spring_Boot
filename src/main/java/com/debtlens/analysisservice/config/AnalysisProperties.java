package com.debtlens.analysisservice.config;

public class AnalysisProperties {
}
/*AnalysisProperties.java

Good idea.

Use it for configuration such as:

repository workspace location
CK tool path
analysis timeout
maximum repository size
cleanup enabled

Then these values live in application.yml rather than being hard-coded.*/